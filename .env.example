APP_NAME=Ultrademy
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost/ultra/public
# Careers portal base URL — path-based today, a real subdomain once the vhost cutover
# happens (docs/architecture/16-careers-portal.md §14). Change this one value when it does;
# nothing in code hard-codes either form.
CAREERS_URL=http://localhost/ultra/public/careers

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=ultrademy
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4
